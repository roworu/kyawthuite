[project]
name = "kinoite-integration-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["."]
addopts = [
    "-ra",
    "--strict-config",
    "--strict-markers",
]
